# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# Node (never track dependency trees)
node_modules/

# Build artifacts
dashboard/dist.tar.gz

# The committed pptx venv skeleton was removed from tracking 2026-09-21
venv-pptx/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/

# SQLite databases
*.db
*.sqlite
*.sqlite3
# SOC runtime state (NOT source)
data/*.jsonl
data/*.offset
data/*.bak.*

# Recorded demo artifacts (regenerate per session; see RUNBOOK for how to capture)
data/demos/

# Runtime artifacts (NOT source): created by soc-manager-mcp /
# realtime-soc-server systemd units' ExecStartPre, mode 0700,
# owned by demo-user. Tracking would create noise on every re-deploy.
scripts/soc/realtime-soc-server-pre-start.sh
scripts/soc/soc-manager-mcp/pre-start.sh
scripts/soc/*-pre-start.sh
